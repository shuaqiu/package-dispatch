define({
    "root" : {
        "title" : "超时警报",
        "desc" : "订单${barCode}(由${senderName}(${senderTel})发给${receiverName}(${receiverTel}))经过${expentTime}分钟仍未送达"
    },
    "en" : true,
    "zh-tw" : true,
});