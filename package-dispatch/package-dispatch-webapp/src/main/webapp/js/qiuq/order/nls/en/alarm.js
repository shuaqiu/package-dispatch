define({
    "title" : "Overtime Alarm",
    "desc.notdelivered" : "The order ${barCode}(sent From ${senderName}(${senderTel}) to ${receiverName}(${receiverTel})) <span style='font-weight: bold;'>is still not arrived</span> over ${expentTime} minutes",
    "desc.notfetch" : "The order (sent From ${senderName}(${senderTel}) to ${receiverName}(${receiverTel})) <span style='font-weight: bold;'>is still not fetch</span> over ${expentTime} minutes",

    "itemTip" : "Single click to show the handle detail; Double click to remove this item."
});